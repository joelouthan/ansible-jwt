---
- hosts: all
  remote_user: jlouthan
  become: yes
  become_method: sudo
  tasks:
      - name: Lock password (Note - this doesn't prevent SSHKey access)
        command: passwd -l srichardson

      - name: Expire account to Jan 2 1970 and remove from all secondary groups
        user: name=srichardson expires=1 groups=

      - name: Remove keys
        file: path=/home/srichardson/.ssh/authorized_keys state=absent
