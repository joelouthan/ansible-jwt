---
- hosts: all:!usmc8
  remote_user: jlouthan
  become: yes
  become_method: sudo
  
  tasks:
      - name:  Find websites
        shell: 'ls -la /var/www/ | grep {{  item  }}'
        with_items:
          - eppc.marines.com
